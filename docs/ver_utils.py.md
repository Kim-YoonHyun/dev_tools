


## 2026-03-20 Version 1.0.0
**Tag:** @Major-Release<br>
**Summary:** 첫 버전업<br>
**Detail:**<br>
- 함수 `get_info_result` 의 일부 연산을 함수 `get_temp_dict` 로 분리
- 내부 연산을 가시성 증가용 함수 `get_info_result` 로 분리
- 함수 `get_info_result` 의 리턴 info 에 track 키 추가
- common_untracked 연산 추가
- 내부 연산을 가시성 증가용 함수 `compare_hash_cache` 로 분리
- track 을 통해 해시 검증 대상 파일인지 확인 기능 추가
- 해시 스키마 변화 탐지 부분 추가
- simple_versioning 용 심플한 문서화 함수
- 내부 연산을 가시성 증가용 함수 `versionup_documenting` 로 분리
- `versionup_documenting` 함수 인자에 check_only 추가
- 버전 기록 md 은 docs_ver 에
- 아카이빙 오류 수정
- 아카이빙 함수 `archiving` 신규 생성


## 2026-03-25 Version 1.0.0
**Tag:** @Major-Release<br>
**Summary:** 첫 버전 업<br>
**Detail:**<br>
- 